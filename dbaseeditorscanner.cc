#include "dbaseeditorscanner.h"

#include <QChar>

namespace dBaseEditor {
namespace Internal {

Scanner::Scanner(const QChar *text, const int length)
{
    Q_UNUSED(text)
    Q_UNUSED(length)
}

}  // Internal
}  // dBaseEditor
